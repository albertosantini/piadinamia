{
    "rules": {
        "users": {
            "$uid": {
                 ".read": "auth != null",
                 ".write": "auth != null && auth.uid == $uid"
            }
        },

        "master": {
            ".read": true,
            ".write": false
        }
    }
}
